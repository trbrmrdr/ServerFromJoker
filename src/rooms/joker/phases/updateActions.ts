import { JokerContext, JokerPlayer } from "../types"
import { IAction } from "../engine/types"

export const updateActions = async (ctx: JokerContext, player: JokerPlayer, action?: IAction) => {
 // TODO
}
